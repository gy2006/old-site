extends ../layouts/users

block content
  h1 Flow.ci
  form#form-signin
    input#login(placeholder='Email or Username', required)
    input#password(placeholder='Password', type='password', required)
    input(type='submit', value='Sign In')
